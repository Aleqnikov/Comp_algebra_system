#ifndef MODULE10_SIMPLIFY_POSTFIX_H
#define MODULE10_SIMPLIFY_POSTFIX_H

#include <string>
#include <vector>

// М10: упрощение 0/1 по постфиксной записи
// Реализовано через М2 + М9 + М4
std::vector<std::string> simplifyPostfix(const std::vector<std::string>& postfix);
std::string simplifyPostfixStr(const std::string& postfixStr);

#endif
